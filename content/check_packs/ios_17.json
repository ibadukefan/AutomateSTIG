{
  "stig_id": "Apple_iOS_17_STIG",
  "platform": "generic",
  "version": "1.0.0",
  "checks": [
    { "vuln_id": "V-261700", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep allowSimple", "pattern": "allowSimple.*false", "is_regex": true }, "expected": { "type": "is_true" }, "description": "Simple passcode must be disabled via MDM profile" },
    { "vuln_id": "V-261701", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep minLength", "pattern": "minLength.*[6-9]|minLength.*[1-9][0-9]", "is_regex": true }, "expected": { "type": "is_true" }, "description": "Minimum passcode length must be 6 or more characters" },
    { "vuln_id": "V-261702", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep maxInactivity", "pattern": "maxInactivity.*[1-5]$", "is_regex": true }, "expected": { "type": "is_true" }, "description": "Auto-lock must activate within 5 minutes of inactivity" },
    { "vuln_id": "V-261703", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep maxFailedAttempts", "pattern": "maxFailedAttempts.*([1-9]|10)$", "is_regex": true }, "expected": { "type": "is_true" }, "description": "Device must wipe after maximum failed passcode attempts" },
    { "vuln_id": "V-261704", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep allowCamera", "pattern": "allowCamera", "is_regex": false }, "expected": { "type": "is_true" }, "description": "Camera restriction must be configurable via MDM profile" },
    { "vuln_id": "V-261705", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep forceEncryptedBackup", "pattern": "forceEncryptedBackup.*true", "is_regex": true }, "expected": { "type": "is_true" }, "description": "Encrypted backups must be enforced" },
    { "vuln_id": "V-261706", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep allowScreenShot", "pattern": "allowScreenShot", "is_regex": false }, "expected": { "type": "is_true" }, "description": "Screenshot restriction must be configurable via MDM profile" },
    { "vuln_id": "V-261707", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep allowUntrustedTLSPrompt", "pattern": "allowUntrustedTLSPrompt.*false", "is_regex": true }, "expected": { "type": "is_true" }, "description": "Untrusted TLS certificate prompts must be disabled" },
    { "vuln_id": "V-261708", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep allowCloudBackup", "pattern": "allowCloudBackup.*false", "is_regex": true }, "expected": { "type": "is_true" }, "description": "iCloud backup must be disabled" },
    { "vuln_id": "V-261709", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep allowCloudDocSync", "pattern": "allowCloudDocSync.*false", "is_regex": true }, "expected": { "type": "is_true" }, "description": "iCloud document sync must be disabled" },
    { "vuln_id": "V-261710", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep allowDiagnosticSubmission", "pattern": "allowDiagnosticSubmission.*false", "is_regex": true }, "expected": { "type": "is_true" }, "description": "Diagnostic data submission must be disabled" },
    { "vuln_id": "V-261711", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep forceAirDropUnmanaged", "pattern": "forceAirDropUnmanaged.*false", "is_regex": true }, "expected": { "type": "is_true" }, "description": "AirDrop from managed apps must be restricted" },
    { "vuln_id": "V-261712", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep allowManagedAppsCloudSync", "pattern": "allowManagedAppsCloudSync.*false", "is_regex": true }, "expected": { "type": "is_true" }, "description": "Managed app cloud sync must be disabled" },
    { "vuln_id": "V-261713", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep requirePasscode", "pattern": "requirePasscode.*true", "is_regex": true }, "expected": { "type": "is_true" }, "description": "Device passcode must be required" },
    { "vuln_id": "V-261714", "platform": "generic", "check": { "type": "command", "command": "profiles list -output stdout-xml | grep allowFingerprintForUnlock", "pattern": "allowFingerprintForUnlock", "is_regex": false }, "expected": { "type": "is_true" }, "description": "Biometric unlock policy must be configured via MDM profile" }
  ]
}
